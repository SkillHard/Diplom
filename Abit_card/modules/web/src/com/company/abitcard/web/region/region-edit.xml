<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://schemas.haulmont.com/cuba/window.xsd"
        caption="msg://editorCaption"
        class="com.company.abitcard.web.region.RegionEdit"
        datasource="regionDs"
        focusComponent="fieldGroup"
        messagesPack="com.company.abitcard.web.region">
    <dsContext>
        <datasource id="regionDs"
                    class="com.company.abitcard.entity.Region"
                    view="region-view"/>
    </dsContext>
    <dialogMode height="600"
                width="800"/>
    <layout expand="windowActions" spacing="true">
        <fieldGroup id="fieldGroup" datasource="regionDs">
            <column width="250px">
                <field property="region"/>
            </column>
        </fieldGroup>
        <frame id="windowActions"
               screen="editWindowActions"/>
    </layout>
</window>
